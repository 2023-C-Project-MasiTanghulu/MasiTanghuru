#ifndef GAME_RULE_H
#define GAME_RULE_H

#include <SFML/Graphics.hpp>
#include "main.h" // Main 클래스 헤더 파일 추가

class Game_Rule {
public:
    void run(sf::RenderWindow& window);

};

#endif // GAME_RULE_H
